Accessible menu-button dropdown: aria-haspopup/expanded, Escape and outside-click close, focus-visible ring.
```jsx
<Dropdown label="Section" items={['Quantitative','Verbal','Data Insights']} onSelect={fn} />
```
Use `Select` for form values; `Dropdown` for actions/navigation. `align="right"` for right-edge placement.
